using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BasicClass : Person, Animal
{
    List<Kewal> kewals = new List<Kewal>();

    Kewal kewal;

    public int count = 0;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // Update is called once per frame
     public void Method1()
    {

    }

    public void animalSound()
    {
        
    }

    public void run()
    {
     
    }
}
